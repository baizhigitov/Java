/*let js = 'amazing';
console.log(40+8+23-10);
console.log('Jonas');
console.log(23);
let firstName = 'Islambek';
console.log(firstName);
*/

/*

// Assignments !!!
// Lecture: Values and Variables
let country = 'Kyrgyzstan';
let continent = 'Central Asia';
let population = 7000000;
console.log(country);
console.log(continent);
console.log(population);
console.log(`${country}'s population is ${population > 33000000 ? 'above' : 'below'} average`);

*/
/*
// Lecture: Data types
let isIsland = 'Central Asia';
let language;
console.log (isIsland, population, country, language);

// Lecture: let , const , var
language = "kyrgyz language";
console.log(isIsland, population, country, language);

const myResult = (isIsland + ' ' + population + ' ' + country + ' ' + language);
console.log(myResult);


// Lecture: Basic operators
let dividedPopulation;
dividedPopulation = population /2 ;
console.log(dividedPopulation);
dividedPopulation ++;
console.log(dividedPopulation);

let myFriendsCountry = 'Finland';
let populationOfFinland = 6000000;
let comparisonOfCountries;
console.log(dividedPopulation >= myFriendsCountry);
comparisonOfCountries = (dividedPopulation >= myFriendsCountry);
let averagePopulation;
averagePopulation = 33000000;
comparisonOfCountries = (dividedPopulation >= averagePopulation);

// Taking Decisions: if / else Statements
let checkAverage;
if (population >= averagePopulation){
    checkAverage = `${country}'s population is above average`;
} else {
    checkAverage = `${country}'s population is below average`;
}
console.log(checkAverage);
console.log(comparisonOfCountries);

console.log(country + ' is in ' + isIsland + ' , and its ' + population + ' people speak ' + language);

let description = (`${country} is in ${isIsland}, and its ${population} people speak ${language}`);

console.log(description);
*/

/*
// Lecture; Equality Operators == vs ===
const numNeighbours = prompt ('How many neighbour countries does your country have?'); 
const answerPromt = numNeighbours;
console.log(Number (answerPromt));
if (numNeighbours === 1){
    console.log('Only 1 border!');
} else if (numNeighbours > 1 ){
    console.log('More than 1 border');
} else {
    console.log('No borders');
}
*/

/*
// Logical Operators
const countryToLive = 'Portugal';
const spokenEnglish = true; 
const numberOfPeople = 35000000;
const notIsland = true; 
if (spokenEnglish === true && numberOfPeople < 50000000 && notIsland === true){
    console.log(`You should live in ${countryToLive}`);
} else {
    console.log(`${countryToLive} does not meet your criteria`);
}
*/


/*
// the switch statement
const language = 'arabic';

switch (language){
    case 'chinese':
    case 'mandarin':
        console.log('MOST number of native speakers!');
    break;
    case 'spanish':
        console.log('2nd place in number of native speakers');
    break;
    case 'english':
        console.log('3rd place');
    break;
    case 'hindi':
        console.log('Number 4');
    break;
    case 'arabic':
        console.log('5th most spoken language');
    break;
    default:
        console.log('Great language too :D');

}

*/


/*
// The conditional (ternary) operator
const countryName = 'Kyrgyzstan';
const numberPopulation = 33000000;
const comparisonOfPopulation = numberPopulation >= 33000000 ? `${countryName}'s population is above average` : `${countryName}'s population is below average`;
console.log(comparisonOfPopulation);

*/
